{
  "name": "elm-pages-app",
  "type": "module",
  "scripts": {
    "postinstall": "elm-tooling install",
    "start": "elm-pages dev",
    "build": "elm-pages build",
    "test": "elm-test",
    "mcp:test": "npx @playwright/mcp@latest",
    "mcp:install": "npm install --save-dev @playwright/mcp",
    "mcp:setup": "npx playwright install chromium"
  },
  "devDependencies": {
    "@playwright/mcp": "^0.0.37",
    "elm-codegen": "^0.6.1",
    "elm-optimize-level-2": "^0.3.5",
    "elm-pages": "3.0.24",
    "elm-review": "^2.12.0",
    "elm-test": "^0.19.1-revision16",
    "elm-tooling": "^1.15.1",
    "lamdera": "^0.19.1-1.3.2",
    "vite": "^6.0.6"
  },
  "dependencies": {
    "@netlify/functions": "^3.0.0"
  }
}
